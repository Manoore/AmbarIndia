import React, { useState } from 'react';
const items=[['Chicken Tikka Masala',22.99],['Saag Paneer',22.99],['Garlic Nan',7.99],['Vegetable Samosa',9.89],['Chicken Biryani',23.99],['Mango Lassi',6.99]];
export default function CounterPOS(){const [cart,setCart]=useState([]);const [type,setType]=useState('Dine-in');const [placed,setPlaced]=useState(false);const total=cart.reduce((s,i)=>s+i[1],0);return <section className="pos"><header><div><p className="eyebrow">Staff counter</p><h2>New walk-in order</h2></div><div className="pos-types">{['Dine-in','Pickup','Phone','QR table'].map(x=><button className={type===x?'active':''} onClick={()=>setType(x)} key={x}>{x}</button>)}</div></header>{placed?<div className="pos-done"><h2>Order sent to kitchen</h2><p>Rewards and order status are now attached to this guest.</p><button onClick={()=>{setCart([]);setPlaced(false)}}>Start another order</button></div>:<div className="pos-grid"><div className="pos-menu"><input placeholder="Search menu"/><div>{items.map(i=><button key={i[0]} onClick={()=>setCart([...cart,i])}><b>{i[0]}</b><span>${i[1].toFixed(2)}</span><small>Add +</small></button>)}</div></div><aside className="pos-cart"><h3>{type} order</h3><input placeholder={type==='Dine-in'?'Table number':'Guest name'} /><input placeholder="Phone number for rewards"/><div className="pos-lines">{cart.map((i,n)=><p key={`${i[0]}${n}`}><span>{i[0]}</span><b>${i[1].toFixed(2)}</b></p>)}</div><p className="pos-total"><span>Total</span><b>${total.toFixed(2)}</b></p><select><option>Card terminal (test)</option><option>Cash</option><option>Split payment</option></select><button disabled={!cart.length} onClick={()=>setPlaced(true)}>Send to kitchen</button></aside></div>}</section>}
